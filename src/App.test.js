import React from "react";
import { render } from "@testing-library/react";
import App from "./App";

it("renders a disabled input", () => {
  // const { getByTestId } = render(<App />);
  // const myInput = getByTestId("myInput");
  // expect(myInput).toBeDisabled();
});
